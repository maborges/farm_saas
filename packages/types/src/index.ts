/**
 * @farm/types — Barrel export
 * Centraliza os tipos inferidos pelo Zod e quaisquer tipos utilitários
 * de negócio que precisam ser compartilhados no workspace.
 */

// Re-exporta tudo de zod-schemas para simplicidade de consumo
export * from "@farm/zod-schemas";

// Futuro: Tipos adicionais (como DTOs específicos de UI) podem entrar aqui
